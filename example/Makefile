include ../common.mk

.PHONY: all test clean

all:
	${MAKE} -C fizzbuzz all
	${MAKE} -C 8cc      all
	${MAKE} -C zlib     all

test:
	${MAKE} -C fizzbuzz test
	${MAKE} -C 8cc      test
	${MAKE} -C zlib     test
